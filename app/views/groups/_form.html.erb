<%= form_for([@group.teacher, @group]) do |f| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group.errors.count, "error") %> prohibited this group from being saved:</h2>
      <ul>
      <% @group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name,"Nazwa" %><br>
    <%= f.text_field :name, :class => "form-control", :placeholder => "Nazwa Grupy" %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit "Stwórz Grupę", :class =>"btn btn-primary"%>
  </div>
<% end %>
