<%= render "panel" %>
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>
 <% i=0%>
  <tbody>
    <% @wyzwania.each do |wyzwanie| %>
      <tr>
          <% if wyzwanie.status==0 %>
            <td><%= i=1+i %></td>
            <td><%= Student.find(wyzwanie.challenger_id).name %></td>
            <td><%= Student.find(wyzwanie.challenger_id).lastname %></td>
            <td><%= link_to "Rozwiaż", student_challengeodp2_path(:idwyzwania => wyzwanie.id)%></td>
            <td><%= link_to "Oddaj walkower", student_surender_path(:idwyzwania => wyzwanie.id)%></td>
          <% end %>
      </tr>
    <% end %>
  </tbody>
</table>